syntax = "proto3";

// Is used by the client to ack an authorative update from the server. Should be called immediately
// after receiving a ServerWorldStateDiff.
message AckUpdatesRequest {
  uint64 world_version = 1;
}

message AckUpdatesResponse {
}
